import { colors } from "../utilities";

const theme = {
    light: {
        theme: 'light',
        text: colors.black,
        background: colors.background,

    },
    dark: {
        theme: 'dark',
        text: colors.white,
        background: colors.black,
    },
}

export default theme